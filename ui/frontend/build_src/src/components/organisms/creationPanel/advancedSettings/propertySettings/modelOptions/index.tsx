import React from "react";
import { useImageCreate } from "../../../../../../stores/imageCreateStore";
import HeadlessListbox, { listBoxOption } from "../../../../../atoms/headlessListbox";



export default function ModelOptions() {
  // const setRequestOption = useImageCreate((state) => state.setRequestOptions);
  // const modelValue = useImageCreate((state) => state.getValueForRequestKey("model"));
  // const handleChange = (option: listBoxOption) => {
  //   setRequestOption("model", option.value);
  // };
  return (
    <div></div>
    // <HeadlessListbox
    //     options={options}
    //     currentOption={selectedUpscaleOption}
    //     handleChange={handleChange}
    //     label={t("settings.model")}
    //     FAIcon={FAIcon}
    // ></HeadlessListbox>
  );
}